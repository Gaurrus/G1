"use strict";

const list = document.querySelector(`#languages`);
console.log(list);
list.hidden = true;
const fullFunction = document.querySelector(`ol>li:first-of-type ol`);
console.log(fullFunction);

fullFunction
document.body.textContent = fullFunction;
